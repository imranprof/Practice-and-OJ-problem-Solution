#include<stdio.h>
#include<math.h>
#include<string.h>

int main()
{
    int i, t_case;
    char *str;
    int n;
    double sqt;

    scanf("%d", &t_case);

    for(i = 1; i <= t_case; i++) {
        scanf("%d", &n);
        sqt = sqrt(n);
        //printf("%.4lf\n", sqt);

        str =
    }

    return 0;
}
