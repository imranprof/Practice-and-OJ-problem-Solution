#include<stdio.h>

int x;  // here x i global variable which value assign 0 by the program

int main()
{
    int y;  // here y i local variable

    printf("x = %d, y = %d\n", x, y);

    return 0;
}
