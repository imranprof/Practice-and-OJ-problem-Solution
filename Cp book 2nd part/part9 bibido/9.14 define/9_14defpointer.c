#include<stdio.h>

int main(int argc, char *argb[])
{
    int * a;    // a is an integer pointer
    int * b;    // b is also an integer pointer
    int * c, d; // c is an integer pointer
                // But, d is only an integer

    return 0;
}
